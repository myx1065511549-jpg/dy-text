# 采集与解析(已用)
requests>=2.32
protobuf>=4.25
mini-racer>=0.12          # 执行抖音官方 webmssdk.js 生成签名
playwright>=1.40          # 浏览器辅助采集(hook WebSocket 捕获真帧)
grpcio-tools>=1.60        # 编译 douyin.proto -> _pb2.py(开发期用)

# 说明:装完 playwright 后需执行 `python -m playwright install chromium` 下载浏览器内核

# 后续模块(存储/服务/看板)按需补充:
# fastapi、uvicorn 等
